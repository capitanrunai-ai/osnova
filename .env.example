# Absolute public origin used while building canonical, Open Graph, sitemap and
# robots URLs. Replace this when a custom domain is connected.
SITE_URL=https://osnova.example.com

# Configure these as protected deployment variables. Never commit real values.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
